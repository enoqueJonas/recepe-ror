<div class="recipe">
  <%= link_to recipe.name, recipe_path(recipe) %>
  <div class="recipe_actions">
    <%= button_to "Delete",
                  recipe_path(recipe),
                  method: :delete,
                  class: "btnDelete" %>
  </div>
</div>
